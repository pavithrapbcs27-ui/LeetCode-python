class Solution:
    def grayCode(self, n: int) -> List[int]:
        # A Gray code sequence can be generated by: G(i) = i ^ (i >> 1)
        # We iterate from 0 to 2^n - 1
        return [i ^ (i >> 1) for i in range(1 << n)]
